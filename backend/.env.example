# Optional: PostgreSQL (async driver). Omit to use in-memory scan storage.
# You can also put DATABASE_URL in the repo root .env.local — the backend loads that file now.
# DATABASE_URL=postgresql+asyncpg://reposcanner:reposcanner@localhost:5432/reposcanner

# FRONTEND_ORIGINS=http://localhost:3000
